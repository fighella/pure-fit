import * as EvolveStyles from './styles';
// eslint-disable-next-line import/prefer-default-export
export const EvolveConfig = {
  zone: 'Etc/GMT',
  rowWidth: '92%',
  style: EvolveStyles.style,
  row_size: 3
};
