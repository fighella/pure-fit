import React from 'react';
import { Container, Row } from 'reactstrap';
import { navHeading } from '../utils/nav';

const NavTeam = () => (
  <Container fluid>
    {navHeading('Meet the team')}
    <Row>Content?</Row>
  </Container>
);

export default NavTeam;
